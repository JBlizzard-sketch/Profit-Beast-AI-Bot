services:
  - type: web
    name: profit-beast-bot
    env: python
    pythonVersion: 3.12.11  # Use a specific patch version for precision
    buildCommand: |
      python -m pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python src/main.py
