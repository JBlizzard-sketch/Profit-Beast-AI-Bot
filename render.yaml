services:
  - type: web
    name: profit-beast-bot
    env: python
    pythonVersion: 3.12.11
    buildCommand: |
      python -m pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python src/app_main.py  # Changed from src/main.py
